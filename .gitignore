# Local scratch — never publish
_leakscan.txt
*.local
.DS_Store
Thumbs.db

# Knowledge-graph tool output (graphify / understand-anything)
graphify-out/
.understand-anything/

# Keep Claude local state out while allowing the committed review policy.
.claude/*
!.claude/review-policy.json

# Semgrep guardian MCP writes a zero-byte config here; local tool state.
.semgrep/guardian.yml
